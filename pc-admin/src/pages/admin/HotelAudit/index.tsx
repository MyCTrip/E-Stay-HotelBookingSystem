import React from 'react';
import { Typography } from 'antd';

const HotelEntry: React.FC = () => {
  return (
    <div style={{ padding: 24 }}>
      <Typography.Title level={2}>酒店信息录入</Typography.Title>
      <p>这里将放置复杂的表单组件...</p>
    </div>
  );
};

export default HotelEntry;
